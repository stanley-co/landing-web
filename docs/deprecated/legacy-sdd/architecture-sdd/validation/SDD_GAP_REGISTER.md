# SDD Gap Register

| ID | Severity | Code evidence | Affected docs | Problem | Correction | Acceptance criteria |
| --- | --- | --- | --- | --- | --- | --- |
| GAP-001 | P0 | `src/types/product.ts`, `ProductDetailPage`, current S3 has `materialsAndNews.atricles` once | backend, contracts, admin, migrator | Related articles may be lost if only `articles` is imported. | Preserve and normalize legacy typo `atricles`; warn in migrator report. | Product related articles render after migration for both spelling variants. |
| GAP-002 | P0 | `src/App.tsx` route `/news/:id`; `NewsArticlePage` searches by `id` | contracts, content, admin | Requiring slug would break existing URLs. | Keep existing ID as public identifier; slug nullable future. | Existing `/news/2025-...` URLs work without slug. |
| GAP-003 | P1 | `EquipmentPage` hardcoded fallback order differs from current S3 categories | catalog, admin categories, migrator | Category model can break grouping/order if it assumes old subcategories. | Import real `globalCategory/category`, store explicit sort order, keep fallback mapping only for legacy missing globalCategory. | Public category tree matches current S3 grouping and counts. |
| GAP-004 | P1 | `ProductSpecs` renders arbitrary `Object.entries(specs)` | product editor, DB, DTO | Fixed spec enum would drop Russian/custom keys. | Specs are ordered key/value rows, not fixed columns. | Any current S3 spec key appears in public detail and admin editor. |
| GAP-005 | P1 | `EquipmentCarousel` schema has no `visible/actionType/adaptiveImages`; order is array order | slides/admin/contracts | Overdesigned slide model can mismatch current DTO. | Target may add status/sortOrder but public DTO remains current-compatible; action behavior derived from link. | Current three S3 slides render from API without frontend rewrite. |
| GAP-006 | P1 | `ArticleBody` supports paragraph/image/quote/link; current S3 has paragraph/image/quote | content/admin/contracts | Single `body` field would lose block structure. | Keep structured `ContentBlock` rows and editor. | Admin creates all supported block types and public detail renders them. |
| GAP-007 | P1 | `CooperationForm` sends Telegram/Bitrix from browser; `ContactForm` alert-only and unused | backend lead/admin/notifications | Lead model must cover real fields and remove browser-side CRM dependency. | Public lead API stores fields, consent, source, optional product; email notification; Telegram stub; Bitrix out of MVP. | Submitting modal/product form creates DB lead and sends email. |
| GAP-008 | P2 | `/home` route exists in `App.tsx` and `HomePage` | landing/backend/page/admin | Removing or ignoring legacy route would violate compatibility. | Document `/home` as legacy route; optional legacy blocks. | `/home` remains accessible after migration. |
| GAP-009 | P2 | About certificates disabled, `/home` certificates placeholder | admin certificates/document | Certificate/PDF source is not current S3 JSON. | Manage metadata + S3 binaries, but mark source scan as required. | Admin can upload/manage certificate PDFs/images once real files are known. |
| GAP-010 | P2 | `npm run build` fails unused imports | landing audit/testing | Current code does not pass build. | Record build failure; production code fix is separate task. | Build task removes unused imports and passes `tsc && vite build`. |
| GAP-011 | P2 | `npm install` reports 21 vulnerabilities | platform/landing risks | Dependency risk not reflected in validation. | Add audit result to landing risk/test docs. | Security triage task exists before release. |
| GAP-012 | P2 | Footer/share/menu URLs are mixed internal/external/frontend-only | admin/settings/contracts | Universal URL registry would overdesign system. | Split business links, site settings, and frontend-only share/route links. | No CRUD screen for all URLs; only owned settings/content links editable. |

After the SDD updates in this validation pass, no P0 remains open in documentation. P1 items are covered by explicit requirements and matrices; implementation still has to satisfy them.
