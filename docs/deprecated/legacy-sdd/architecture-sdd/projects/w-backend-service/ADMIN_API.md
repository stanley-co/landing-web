# Admin API

CRUD for products, categories, content, pages, legacy, slides, contacts, menu, media, documents, certificates, leads, users, audit, settings.

Admin endpoints:

- require auth;
- enforce permissions;
- validate input;
- write audit for changes;
- support pagination, filtering and sorting.

