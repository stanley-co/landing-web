# Project Context

`w-backend-service` serves public API for `landing-web`, admin API for `w-admin-web`, auth API, data storage, notifications, audit and migration support.

It owns PostgreSQL schema and business rules after migration.

