# Audit

Audit all admin writes, login/logout, file operations, lead status changes and email retry.

Store old/new JSON snapshots or compact diffs. Include actor, action, entity, timestamp, IP, user-agent and requestId.

