# Agent Instructions

Use S3 as source of truth. Preserve IDs. Implement dry-run first. Do not mutate S3. Add Testcontainers tests. Produce migration report for every run.

Result: changed files, task IDs, dry-run/import output, tests, broken references, next task.

