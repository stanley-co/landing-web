# Target Data

Target: PostgreSQL tables owned by `w-backend-service`.

Structured data: products, categories, specs, advantages, content, blocks, slides, pages, contacts, documents/certificates metadata.

Binaries remain in S3.

