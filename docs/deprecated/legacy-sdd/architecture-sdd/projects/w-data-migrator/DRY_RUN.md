# Dry Run

Dry-run performs all reads, validation and mapping without database writes.

Report includes counts, would-create/would-update/would-skip, broken media, duplicate IDs, unknown categories and content relation failures.

