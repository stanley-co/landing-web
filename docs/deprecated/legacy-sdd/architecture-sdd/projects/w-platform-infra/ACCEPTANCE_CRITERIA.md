# Acceptance Criteria

- `docker compose up` starts local dependencies.
- MinIO bucket is created.
- Backend health is green.
- Mailpit receives test email.
- Frontends can reach API.
- VPS baseline avoids Kubernetes.

