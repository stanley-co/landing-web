# Dashboard

Route: `/dashboard` and `/`. Roles: all authenticated.

Data: product counts, active materials, legacy blocks, new leads, email errors, last changes, latest PDFs, quick actions.

Endpoints: admin summary endpoints or aggregated dashboard endpoint.

Acceptance: shows only metrics useful to project; actions hidden by permissions.

