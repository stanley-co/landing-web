# Settings

Route: `/settings`.

Sections: site settings, analytics, contacts shortcuts, email notification settings, upload limits read-only.

Secrets/security settings visible/editable only by `ADMIN`; sensitive values masked.

