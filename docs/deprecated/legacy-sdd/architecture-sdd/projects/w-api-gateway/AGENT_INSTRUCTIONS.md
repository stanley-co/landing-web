# Agent Instructions

For MVP, implement these requirements inside `w-backend-service`. Do not create a Spring Cloud Gateway app unless explicitly requested by ADR update.

Follow shared `ERROR_MODEL`, `SECURITY_BASELINE`, `API_CONVENTIONS`.

Result format: changed files, implemented gateway task IDs, tests, limitations, next task.

