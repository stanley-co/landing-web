# Acceptance Criteria

- All API routes use `/api/v1/*`.
- CORS rejects unknown origins.
- Admin routes require auth.
- Lead/login rate limits work.
- Upload limits are enforced.
- Errors include requestId.
- No separate gateway app is required in MVP.

