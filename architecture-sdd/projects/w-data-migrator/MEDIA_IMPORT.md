# Media Import

Do not copy binaries unless required. Import metadata for referenced S3 objects.

Fields: bucket, key, public URL, MIME, size, checksum if available, original filename, status, usage relation.

