# Configuration

Environment variables:

- DB URL/user/password;
- S3 endpoint/bucket/access/secret;
- SMTP host/port/user/password/from/to;
- CORS origins;
- token secrets/lifetimes;
- upload limits;
- admin bootstrap credentials for local only.

No secrets in repository or frontend.

