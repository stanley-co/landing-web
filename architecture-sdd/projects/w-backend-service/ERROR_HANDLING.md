# Error Handling

Use shared error model. Implement `@ControllerAdvice` for validation, auth, not found, conflicts, business rule errors, upload errors and unexpected errors.

Every error includes requestId. No stack traces in response.

