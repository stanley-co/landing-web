# Certificates

Route: `/certificates`.

Fields: number, title, issue date, expiry date, PDF, visibility, sort order, relation.

Acceptance: invalid date range rejected; public active certificates visible.

