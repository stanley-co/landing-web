# Layout

Use Ant Design `Layout` with sidebar navigation, top bar user menu, breadcrumbs and content area.

Mobile support: responsive collapse; admin can be desktop-first but must remain usable on tablets.

