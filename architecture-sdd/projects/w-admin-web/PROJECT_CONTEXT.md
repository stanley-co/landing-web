# Project Context

`w-admin-web` consumes `/api/v1/auth/**` and `/api/v1/admin/**`. It does not call S3 directly; all files are uploaded through backend.

Roles: `ADMIN`, `FEATURE_OWNER`, `CONTENT_READER`.

