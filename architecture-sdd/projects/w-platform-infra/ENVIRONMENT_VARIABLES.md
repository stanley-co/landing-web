# Environment Variables

Templates required:

- `.env.example`;
- `backend.env.example`;
- `frontend.env.example`;
- `admin.env.example`.

Include DB, S3, SMTP, CORS, token secrets, upload limits, API base URLs.

