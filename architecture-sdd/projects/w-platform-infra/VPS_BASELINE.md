# VPS Baseline

Future production on own VPS:

- Docker and Docker Compose;
- reverse proxy with HTTPS;
- PostgreSQL;
- backend;
- static public/admin frontends;
- object storage access;
- backups;
- log rotation;
- health checks.

No Kubernetes, service mesh or multi-region in MVP.

