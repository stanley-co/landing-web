# Public Routes

Public endpoints include site, menu, pages, products, categories, content, news by ID, slides, contacts, documents, certificates and leads.

`POST /api/v1/public/leads` must have stricter rate limit and body validation.

