# w-api-gateway SDD

MVP decision: no separate deployable gateway repository/application. This package describes gateway/edge functions implemented inside `w-backend-service`.

If extraction criteria are met later, this folder can seed a standalone Spring Cloud Gateway project.

