# Security Headers

Set:

- `X-Content-Type-Options: nosniff`;
- `X-Frame-Options: SAMEORIGIN` or CSP frame policy;
- `Referrer-Policy`;
- `Permissions-Policy`;
- HSTS in production HTTPS;
- CSP baseline, allow Rutube embeds only where required by frontend.

